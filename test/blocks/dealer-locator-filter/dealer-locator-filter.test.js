/* eslint-disable no-unused-expressions */
/* global describe, it */

// eslint-disable-next-line import/no-unresolved
import { expect } from '@esm-bundle/chai';
// eslint-disable-next-line import/no-extraneous-dependencies, import/no-unresolved
import { readFile } from '@web/test-runner-commands';
import decorate from '../../../blocks/dealer-locator-filter/dealer-locator-filter.js';

// document.write(await readFile({ path: './dealer-locator-filter.plain.html' }));

// const block = document.querySelector('.dealer-locator-filter');
// await decorate(block);

// describe('Dealer Locator Filter Block', () => {
//   it('Should render dealer locator', async () => {
//     expect(block.querySelectorAll('.dealer-locator').length).to.equal(1);
//   });
// });
